# Build and local tooling
dist/
*.luac

# Editors
.idea/
*.swp
.DS_Store

# Local server configuration: contains credentials
server.jsonc
acl.jsonc
.env

# Agent tooling: 16 MB of skills, two of them nested git repos
.claude/

# docs/ is published: docs/architecture.md is the design rationale and the
# README points at it, so a clone without it is missing the explanation.
